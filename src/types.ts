export interface StatsType {
  icon: string;
  no: string;
  title: string;
}

export interface SyllabusProgressBarType {
  title: string;
  progress: number;
}

export interface UserInputs {
  rate: number;
  percentile: number;
  score: number;
}
